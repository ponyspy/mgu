extends ../layout
include ../exercises

block title
	title cool!

block content_style
	link(rel='stylesheet', type='text/css', href='/styles/main.css' + version)

block content_scripts
	script(type='text/javascript', src='/scripts/main.js' + version)

block content
	h1 cool
	a(href='/') назад
	br
	b= locale
	br
	a(href='/lang/ru') ru
	!= ' | '
	a(href='/lang/en') en
	br
	br
	+ex1()
	+ex2()
	each test in tests
		b=test.title[locale] || test.title.i18n
		br
		b=test.description[locale] || test.description.i18n
		br
		br